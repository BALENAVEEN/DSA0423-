import pandas as pd

data = {
    "Product": ["Pen", "Book", "Pen", "Pencil", "Book", "Pen", "Eraser"]
}

df = pd.DataFrame(data)

top_products = df['Product'].value_counts().head(5)

print("Top 5 Products Sold:")
print(top_products)
