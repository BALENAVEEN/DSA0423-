import pandas as pd
from sklearn.cluster import KMeans

data = {
'CustomerID':[1,2,3,4,5],
'AmountSpent':[200,500,300,800,700],
'Visits':[5,10,6,15,12]
}

df = pd.DataFrame(data)

X = df[['AmountSpent','Visits']]

kmeans = KMeans(n_clusters=2)
df['Cluster'] = kmeans.fit_predict(X)

print(df)
